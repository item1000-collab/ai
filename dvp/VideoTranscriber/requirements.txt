# ============================================================
# VIDEO TRANSCRIBER - ЗАВИСИМОСТИ
# ============================================================

# Core dependencies
streamlit>=1.30.0
moviepy>=1.0.3
openai-whisper>=20231117
requests>=2.28.0
humanize>=4.6.0

# Transformers ecosystem
transformers>=4.35.0,<5.0.0
tokenizers>=0.14.0

# ML dependencies
numpy>=1.24.0
scipy>=1.10.0
scikit-learn>=1.3.0

# Audio processing and ML models
speechbrain>=1.0.0
pyannote.audio>=3.1.1
pytorch-lightning>=2.0.0

# Other dependencies
iso639>=0.1.4
protobuf>=5.0.0
matplotlib>=3.5.0

# ============================================================
# АУДИО-БИБЛИОТЕКИ ДЛЯ КОНВЕРТАЦИИ (НЕ ТРЕБУЮТ FFMPEG)
# ============================================================
# librosa - загрузка аудио (поддерживает MP3, M4A, FLAC, OGG)
librosa>=0.10.0

# soundfile - запись WAV файлов
soundfile>=0.12.1

# audioread - бэкенд для librosa (читает MP3 без FFmpeg)
audioread>=3.0.0

# ============================================================
# ОПЦИОНАЛЬНО: ДЛЯ РАБОТЫ С АУДИО
# ============================================================
# ffmpeg-python - обертка для FFmpeg (опционально, если FFmpeg установлен в системе)
# ffmpeg-python>=0.2.0

# ============================================================
# ОПЦИОНАЛЬНО: ДЛЯ РАБОТЫ С OLLAMA
# ============================================================
# ollama

# ============================================================
# ПРИМЕЧАНИЯ ПО УСТАНОВКЕ:
# ============================================================
# 1. PyTorch устанавливается отдельно (не включен в requirements.txt):
#    Для CUDA: pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
#    Для CPU: pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu
#
# 2. Для pyannote.audio нужен HuggingFace токен с доступом к:
#    https://huggingface.co/pyannote/speaker-diarization-3.0
#
# 3. FFmpeg не требуется для конвертации MP3 (используется librosa + audioread)
